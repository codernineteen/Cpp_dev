#include <iostream> //load built-in lib, here no need to add semicolon

// main function - starting point
int main()
{
    /*
        std::cout, std::endl coming from iostream
        need semicolon at the end of the line
        endl - end of line  and create new line
    */
    std::cout << "Hello, World!" << std::endl;

    // let the OS know whether there is a problem or not during compile process
    return 0;
}